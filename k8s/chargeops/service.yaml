apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: chargeops
  labels:
    app: ai-service
spec:
  type: ClusterIP
  selector:
    app: ai-service
  ports:
    - name: http
      port: 8008
      targetPort: http
